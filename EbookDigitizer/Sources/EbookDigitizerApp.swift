import SwiftUI
import EbookDigitizerCore

@main
struct EbookDigitizerApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Canvas Ready")
                .frame(width: 800, height: 600)
        }
    }
}
